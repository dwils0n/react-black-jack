import React from 'react';
import { shallow } from 'enzyme';
import Card from './PreGameInfo';

const props = {
    cardId: '2',
    cardSuit: 'diamonds'
}

it('renders correctly', () => {
    //const component = shallow(<Card {...props} />);

    //expect(component.find('.game').length).toBe(1);
});